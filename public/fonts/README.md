# Custom Typeface

Drop project font files in this folder.

Recommended filename for the app-wide typeface:

```text
C:\Users\email\Documents\Traces\public\fonts\traces.ttf
```

After the file is present, the CSS can be switched from the fallback monospace stack to that font without moving the asset.
